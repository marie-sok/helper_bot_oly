# Telegram Bot Token
telegram.bot.token=7352300616:AAE2NqlvEovQ12kkPJtkvdQV6zmIS8svoBU

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/telegram_bot
spring.datasource.username=helper_oly
spring.datasource.password=mashmallow
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Liquibase Configuration
spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.yaml

# Logging Configuration
logging.level.com.example.reminder_telegram_bot=DEBUG
logging.level.org.springframework.boot.autoconfigure.liquibase=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# Server Configuration
server.port=8081